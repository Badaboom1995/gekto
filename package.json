{
  "name": "gekto",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n test,widget,proxy -c blue,green,yellow \"bun --filter test-app dev\" \"bun --filter widget dev\" \"bun --filter @gekto/server dev\"",
    "dev:test-app": "bun --filter test-app dev",
    "dev:widget": "bun --filter widget dev",
    "dev:proxy": "bun --filter @gekto/server dev",
    "preview": "bun run build && concurrently -n test,proxy -c blue,yellow \"bun --filter test-app dev\" \"bun --filter @gekto/server preview\"",
    "build": "bun --filter widget build && bun --filter @gekto/server build",
    "bundle": "bun run build && bun run scripts/bundle.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
