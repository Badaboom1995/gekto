/**
 * Game interface for arcade gallery
 * Local stub type definition for this task
 */
export interface Game {
  id: string;
  name: string;
  available: boolean;
  description?: string;
  coverColor?: string;
}
